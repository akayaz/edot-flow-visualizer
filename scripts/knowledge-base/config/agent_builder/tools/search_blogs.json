{
  "id": "search_blogs",
  "type": "esql",
  "description": "Search Elastic and OpenTelemetry blog posts and tutorials for announcements, deep dives, and practical guides. Use when the user asks about recent developments, use cases, or step-by-step tutorials.",
  "configuration": {
    "query": "FROM edot-assistant-blogs | WHERE body_semantic MATCH ?query | KEEP title, body, url, tags, last_crawled | SORT _score DESC | LIMIT 5"
  },
  "params": {
    "query": {
      "type": "keyword",
      "description": "The user's search query for blog posts or tutorials"
    }
  }
}
