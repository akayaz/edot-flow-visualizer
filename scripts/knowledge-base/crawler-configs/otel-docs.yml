# Elastic Open Web Crawler config for OpenTelemetry docs.
# Index target: edot-kb-docs

output_sink: elasticsearch
output_index: edot-kb-docs

max_crawl_depth: 5
max_unique_url_count: 3000
max_body_size: 5_242_880
purge_crawl_enabled: false
threads_per_crawl: 6
stats_dump_interval: 20

elasticsearch:
  host: <%= ENV['ES_HOST'] %>
  port: 443
  api_key: <%= ENV['ES_API_KEY'] %>
  pipeline: edot-assistant-processing
  pipeline_enabled: true

domains:
  - url: https://opentelemetry.io
    seed_urls:
      - https://opentelemetry.io/docs
      - https://opentelemetry.io/docs/collector
      - https://opentelemetry.io/docs/collector/configuration
      - https://opentelemetry.io/docs/concepts
      - https://opentelemetry.io/docs/languages
      - https://opentelemetry.io/docs/kubernetes
      - https://opentelemetry.io/docs/specs/otel
    sitemap_urls:
      - https://opentelemetry.io/sitemap.xml

    exclude_tags:
      - header
      - footer
      - nav

    crawl_rules:
      - policy: allow
        type: begins
        pattern: /docs/
      - policy: deny
        type: regex
        pattern: .*
